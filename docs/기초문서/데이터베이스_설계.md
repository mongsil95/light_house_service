# 등대지기 서비스 데이터베이스 설계

> **작성일**: 2026년 1월 16일  
> **담당**: DB 별도 팀  
> **프론트엔드 연동**: 프론트엔드 팀은 API 엔드포인트만 사용

---

## 📌 개요

이 문서는 등대지기 서비스의 데이터베이스 설계 명세서입니다.  
프론트엔드 개발자는 직접 DB에 접근하지 않으며, REST API를 통해서만 데이터를 처리합니다.

---

## 🎯 주요 기능 영역

### 1. 반려해변입양 시스템

- 입양 신청 관리
- 활동 이력 추적
- 정화량 데이터 집계

### 2. 해봄 프로그램

- 프로그램 신청 관리
- 참여자 관리
- 인증서 발급

### 3. 커뮤니티 플랫폼

- 게시판 시스템
- 댓글/좋아요 기능
- 파일 첨부

### 4. 전문가 Q&A

- 질문/답변 시스템
- 전문가 프로필 관리
- 베스트 질문 선정

### 5. 참여광장 (퀴즈/투표)

- 퀴즈 출제 및 응답 수집
- 투표 시스템
- 참여 통계

### 6. 콘텐츠 관리

- 교육 자료
- 뉴스/공지사항
- 이벤트
- **가이드 시스템** (NEW)

### 7. 사용자 관리

- 회원 정보
- 권한 관리
- 활동 이력

---

## 📊 데이터베이스 스키마

### 1. 사용자 관리

#### `users` - 사용자 테이블

```sql
-- TODO: DB팀 - 사용자 테이블 생성
CREATE TABLE users (
  id BIGINT PRIMARY KEY AUTO_INCREMENT,
  email VARCHAR(255) UNIQUE NOT NULL,
  password_hash VARCHAR(255) NOT NULL,
  name VARCHAR(100) NOT NULL,
  phone VARCHAR(20),
  organization VARCHAR(200), -- 소속 기관/단체
  user_type ENUM('individual', 'organization', 'expert', 'admin') DEFAULT 'individual',
  profile_image VARCHAR(500),
  is_active BOOLEAN DEFAULT TRUE,
  email_verified BOOLEAN DEFAULT FALSE,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  last_login_at TIMESTAMP NULL,

  INDEX idx_email (email),
  INDEX idx_user_type (user_type),
  INDEX idx_created_at (created_at)
);
```

**필드 설명**:

- `user_type`: 개인(individual), 기관(organization), 전문가(expert), 관리자(admin)
- `organization`: 학교, 기업, NGO 등 소속 정보

---

### 2. 반려해변입양 (Adopt-a-Beach)

#### `beach_adoptions` - 해변 입양 신청

```sql
-- TODO: DB팀 - 반려해변입양 테이블 생성
CREATE TABLE beach_adoptions (
  id BIGINT PRIMARY KEY AUTO_INCREMENT,
  user_id BIGINT NOT NULL,
  beach_name VARCHAR(200) NOT NULL, -- 입양한 해변 이름
  location VARCHAR(500) NOT NULL, -- 위치 (주소)
  latitude DECIMAL(10, 8), -- 위도
  longitude DECIMAL(11, 8), -- 경도
  adoption_date DATE NOT NULL, -- 입양 신청일
  status ENUM('pending', 'approved', 'active', 'inactive', 'rejected') DEFAULT 'pending',
  participants_count INT DEFAULT 1, -- 참여 인원
  contact_name VARCHAR(100),
  contact_phone VARCHAR(20),
  contact_email VARCHAR(255),
  motivation TEXT, -- 입양 동기
  plan TEXT, -- 활동 계획
  approved_at TIMESTAMP NULL,
  approved_by BIGINT NULL, -- 승인한 관리자 ID
  notes TEXT, -- 관리자 메모
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,

  FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE,
  FOREIGN KEY (approved_by) REFERENCES users(id) ON DELETE SET NULL,
  INDEX idx_status (status),
  INDEX idx_adoption_date (adoption_date),
  INDEX idx_beach_name (beach_name)
);
```

#### `cleanup_activities` - 정화 활동 기록

```sql
-- TODO: DB팦 - 정화 활동 기록 테이블 생성
CREATE TABLE cleanup_activities (
  id BIGINT PRIMARY KEY AUTO_INCREMENT,
  adoption_id BIGINT NOT NULL,
  activity_date DATE NOT NULL,
  participants INT NOT NULL, -- 참여 인원
  duration_minutes INT, -- 활동 시간 (분)
  waste_collected_kg DECIMAL(8, 2), -- 수거한 쓰레기 무게 (kg)
  plastic_kg DECIMAL(8, 2),
  glass_kg DECIMAL(8, 2),
  metal_kg DECIMAL(8, 2),
  general_waste_kg DECIMAL(8, 2),
  photos JSON, -- 활동 사진 URL 배열
  description TEXT,
  weather VARCHAR(50), -- 날씨
  temperature INT, -- 온도
  created_by BIGINT NOT NULL,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,

  FOREIGN KEY (adoption_id) REFERENCES beach_adoptions(id) ON DELETE CASCADE,
  FOREIGN KEY (created_by) REFERENCES users(id) ON DELETE CASCADE,
  INDEX idx_activity_date (activity_date),
  INDEX idx_adoption_id (adoption_id)
);
```

---

### 3. 해봄 프로그램

#### `haebom_programs` - 프로그램 정보

```sql
-- TODO: DB팀 - 해봄 프로그램 테이블 생성
CREATE TABLE haebom_programs (
  id BIGINT PRIMARY KEY AUTO_INCREMENT,
  title VARCHAR(200) NOT NULL,
  description TEXT,
  program_type ENUM('education', 'experience', 'campaign', 'special') NOT NULL,
  target_audience VARCHAR(200), -- 대상 (초등학생, 중학생, 일반인 등)
  capacity INT, -- 정원
  location VARCHAR(500),
  start_date DATE,
  end_date DATE,
  registration_start DATE,
  registration_end DATE,
  status ENUM('upcoming', 'open', 'closed', 'completed', 'cancelled') DEFAULT 'upcoming',
  thumbnail VARCHAR(500),
  requirements TEXT, -- 참가 요건
  materials TEXT, -- 준비물
  price DECIMAL(10, 2) DEFAULT 0.00, -- 참가비
  organizer VARCHAR(200), -- 주최
  instructor VARCHAR(200), -- 강사/진행자
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,

  INDEX idx_status (status),
  INDEX idx_start_date (start_date),
  INDEX idx_program_type (program_type)
);
```

#### `haebom_registrations` - 프로그램 신청

```sql
-- TODO: DB팀 - 프로그램 신청 테이블 생성
CREATE TABLE haebom_registrations (
  id BIGINT PRIMARY KEY AUTO_INCREMENT,
  program_id BIGINT NOT NULL,
  user_id BIGINT NOT NULL,
  participant_name VARCHAR(100) NOT NULL,
  participant_age INT,
  participant_grade VARCHAR(50), -- 학년
  guardian_name VARCHAR(100), -- 보호자 이름 (미성년자인 경우)
  guardian_phone VARCHAR(20),
  group_name VARCHAR(200), -- 단체명 (단체 신청인 경우)
  group_size INT DEFAULT 1,
  status ENUM('pending', 'confirmed', 'waiting', 'cancelled', 'completed') DEFAULT 'pending',
  special_notes TEXT, -- 특이사항
  attended BOOLEAN DEFAULT FALSE, -- 참석 여부
  certificate_issued BOOLEAN DEFAULT FALSE, -- 인증서 발급 여부
  certificate_number VARCHAR(50) UNIQUE,
  registered_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,

  FOREIGN KEY (program_id) REFERENCES haebom_programs(id) ON DELETE CASCADE,
  FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE,
  UNIQUE KEY unique_user_program (user_id, program_id),
  INDEX idx_status (status),
  INDEX idx_program_id (program_id)
);
```

---

### 4. 커뮤니티 (게시판)

#### `boards` - 게시판 설정

```sql
-- TODO: DB팀 - 게시판 테이블 생성
CREATE TABLE boards (
  id BIGINT PRIMARY KEY AUTO_INCREMENT,
  name VARCHAR(100) NOT NULL,
  slug VARCHAR(100) UNIQUE NOT NULL, -- URL 경로용
  description TEXT,
  board_type ENUM('adoption_community', 'haebom_community', 'notice', 'faq', 'general') NOT NULL,
  allow_anonymous BOOLEAN DEFAULT FALSE,
  allow_files BOOLEAN DEFAULT TRUE,
  posts_count INT DEFAULT 0,
  is_active BOOLEAN DEFAULT TRUE,
  display_order INT DEFAULT 0,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,

  INDEX idx_slug (slug),
  INDEX idx_board_type (board_type)
);
```

#### `posts` - 게시글

```sql
-- TODO: DB팀 - 게시글 테이블 생성
CREATE TABLE posts (
  id BIGINT PRIMARY KEY AUTO_INCREMENT,
  board_id BIGINT NOT NULL,
  user_id BIGINT,
  title VARCHAR(300) NOT NULL,
  content TEXT NOT NULL,
  author_name VARCHAR(100), -- 익명 게시 시 사용
  is_anonymous BOOLEAN DEFAULT FALSE,
  is_notice BOOLEAN DEFAULT FALSE, -- 공지글 여부
  is_featured BOOLEAN DEFAULT FALSE, -- 추천/베스트 글
  views_count INT DEFAULT 0,
  likes_count INT DEFAULT 0,
  comments_count INT DEFAULT 0,
  status ENUM('draft', 'published', 'hidden', 'deleted') DEFAULT 'published',
  tags JSON, -- 태그 배열
  published_at TIMESTAMP NULL,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,

  FOREIGN KEY (board_id) REFERENCES boards(id) ON DELETE CASCADE,
  FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE SET NULL,
  INDEX idx_board_id (board_id),
  INDEX idx_status (status),
  INDEX idx_created_at (created_at),
  INDEX idx_is_notice (is_notice),
  FULLTEXT INDEX ft_title_content (title, content)
);
```

#### `post_attachments` - 게시글 첨부파일

```sql
-- TODO: DB팀 - 첨부파일 테이블 생성
CREATE TABLE post_attachments (
  id BIGINT PRIMARY KEY AUTO_INCREMENT,
  post_id BIGINT NOT NULL,
  file_name VARCHAR(255) NOT NULL,
  file_path VARCHAR(500) NOT NULL,
  file_size BIGINT, -- bytes
  file_type VARCHAR(100),
  mime_type VARCHAR(100),
  download_count INT DEFAULT 0,
  uploaded_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,

  FOREIGN KEY (post_id) REFERENCES posts(id) ON DELETE CASCADE,
  INDEX idx_post_id (post_id)
);
```

#### `comments` - 댓글

```sql
-- TODO: DB팀 - 댓글 테이블 생성
CREATE TABLE comments (
  id BIGINT PRIMARY KEY AUTO_INCREMENT,
  post_id BIGINT NOT NULL,
  user_id BIGINT,
  parent_comment_id BIGINT NULL, -- 대댓글용
  content TEXT NOT NULL,
  is_anonymous BOOLEAN DEFAULT FALSE,
  author_name VARCHAR(100),
  likes_count INT DEFAULT 0,
  status ENUM('active', 'hidden', 'deleted') DEFAULT 'active',
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,

  FOREIGN KEY (post_id) REFERENCES posts(id) ON DELETE CASCADE,
  FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE SET NULL,
  FOREIGN KEY (parent_comment_id) REFERENCES comments(id) ON DELETE CASCADE,
  INDEX idx_post_id (post_id),
  INDEX idx_parent_comment_id (parent_comment_id),
  INDEX idx_created_at (created_at)
);
```

---

### 5. 전문가 Q&A

#### `experts` - 전문가 프로필

```sql
-- TODO: DB팀 - 전문가 프로필 테이블 생성
CREATE TABLE experts (
  id BIGINT PRIMARY KEY AUTO_INCREMENT,
  user_id BIGINT UNIQUE NOT NULL,
  organization VARCHAR(200) NOT NULL, -- 소속 (해양환경공단, 국립해양환경공단 등)
  position VARCHAR(100), -- 직책
  specialty VARCHAR(200), -- 전문 분야
  bio TEXT, -- 약력
  profile_image VARCHAR(500),
  is_verified BOOLEAN DEFAULT FALSE, -- 인증 여부
  answers_count INT DEFAULT 0,
  rating DECIMAL(3, 2) DEFAULT 0.00, -- 평점 (0.00 ~ 5.00)
  is_active BOOLEAN DEFAULT TRUE,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,

  FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE,
  INDEX idx_specialty (specialty),
  INDEX idx_is_active (is_active)
);
```

#### `questions` - 질문

```sql
-- TODO: DB팀 - 질문 테이블 생성
CREATE TABLE questions (
  id BIGINT PRIMARY KEY AUTO_INCREMENT,
  user_id BIGINT NOT NULL,
  category VARCHAR(100) NOT NULL, -- 반려해변입양, 해봄 프로그램, 운영·기타 등
  title VARCHAR(300) NOT NULL,
  content TEXT NOT NULL,
  is_anonymous BOOLEAN DEFAULT FALSE,
  status ENUM('pending', 'answered', 'closed') DEFAULT 'pending',
  views_count INT DEFAULT 0,
  likes_count INT DEFAULT 0,
  is_featured BOOLEAN DEFAULT FALSE, -- 인기 질문
  tags JSON,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,

  FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE,
  INDEX idx_category (category),
  INDEX idx_status (status),
  INDEX idx_created_at (created_at),
  INDEX idx_is_featured (is_featured),
  FULLTEXT INDEX ft_title_content (title, content)
);
```

#### `answers` - 답변

```sql
-- TODO: DB팀 - 답변 테이블 생성
CREATE TABLE answers (
  id BIGINT PRIMARY KEY AUTO_INCREMENT,
  question_id BIGINT NOT NULL,
  expert_id BIGINT NOT NULL,
  content TEXT NOT NULL,
  is_accepted BOOLEAN DEFAULT FALSE, -- 채택 여부
  likes_count INT DEFAULT 0,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,

  FOREIGN KEY (question_id) REFERENCES questions(id) ON DELETE CASCADE,
  FOREIGN KEY (expert_id) REFERENCES experts(id) ON DELETE CASCADE,
  INDEX idx_question_id (question_id),
  INDEX idx_expert_id (expert_id),
  INDEX idx_created_at (created_at)
);
```

---

### 6. 참여광장 (퀴즈/투표)

#### `quizzes` - 퀴즈

```sql
-- TODO: DB팀 - 퀴즈 테이블 생성
CREATE TABLE quizzes (
  id BIGINT PRIMARY KEY AUTO_INCREMENT,
  title VARCHAR(200) NOT NULL,
  description TEXT,
  thumbnail VARCHAR(500),
  quiz_type ENUM('single', 'multiple', 'ox') DEFAULT 'single', -- 단일선택, 다중선택, OX
  difficulty ENUM('easy', 'medium', 'hard') DEFAULT 'easy',
  category VARCHAR(100), -- 해양쓰레기, 분리배출, 생태계 등
  question TEXT NOT NULL,
  options JSON NOT NULL, -- 선택지 배열 [{"id": 1, "text": "선택지1"}, ...]
  correct_answer JSON NOT NULL, -- 정답 ID 배열 [1, 3]
  explanation TEXT, -- 해설
  points INT DEFAULT 10, -- 획득 포인트
  start_date TIMESTAMP,
  end_date TIMESTAMP,
  status ENUM('draft', 'active', 'closed') DEFAULT 'draft',
  participants_count INT DEFAULT 0,
  created_by BIGINT NOT NULL,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,

  FOREIGN KEY (created_by) REFERENCES users(id) ON DELETE CASCADE,
  INDEX idx_status (status),
  INDEX idx_start_date (start_date),
  INDEX idx_category (category)
);
```

#### `quiz_responses` - 퀴즈 응답

```sql
-- TODO: DB팀 - 퀴즈 응답 테이블 생성
CREATE TABLE quiz_responses (
  id BIGINT PRIMARY KEY AUTO_INCREMENT,
  quiz_id BIGINT NOT NULL,
  user_id BIGINT NOT NULL,
  selected_answer JSON NOT NULL, -- 선택한 답변 ID 배열
  is_correct BOOLEAN,
  points_earned INT DEFAULT 0,
  time_taken_seconds INT, -- 소요 시간
  submitted_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,

  FOREIGN KEY (quiz_id) REFERENCES quizzes(id) ON DELETE CASCADE,
  FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE,
  UNIQUE KEY unique_user_quiz (user_id, quiz_id),
  INDEX idx_quiz_id (quiz_id),
  INDEX idx_submitted_at (submitted_at)
);
```

#### `polls` - 투표

```sql
-- TODO: DB팀 - 투표 테이블 생성
CREATE TABLE polls (
  id BIGINT PRIMARY KEY AUTO_INCREMENT,
  title VARCHAR(200) NOT NULL,
  description TEXT,
  poll_type ENUM('single', 'multiple') DEFAULT 'single',
  options JSON NOT NULL, -- 투표 선택지
  allow_multiple_votes BOOLEAN DEFAULT FALSE,
  start_date TIMESTAMP,
  end_date TIMESTAMP,
  status ENUM('draft', 'active', 'closed') DEFAULT 'draft',
  total_votes INT DEFAULT 0,
  created_by BIGINT NOT NULL,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,

  FOREIGN KEY (created_by) REFERENCES users(id) ON DELETE CASCADE,
  INDEX idx_status (status),
  INDEX idx_start_date (start_date)
);
```

#### `poll_votes` - 투표 응답

```sql
-- TODO: DB팀 - 투표 응답 테이블 생성
CREATE TABLE poll_votes (
  id BIGINT PRIMARY KEY AUTO_INCREMENT,
  poll_id BIGINT NOT NULL,
  user_id BIGINT NOT NULL,
  selected_options JSON NOT NULL, -- 선택한 옵션 ID 배열
  voted_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,

  FOREIGN KEY (poll_id) REFERENCES polls(id) ON DELETE CASCADE,
  FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE,
  UNIQUE KEY unique_user_poll (user_id, poll_id),
  INDEX idx_poll_id (poll_id),
  INDEX idx_voted_at (voted_at)
);
```

---

### 7. 콘텐츠 관리

#### `educational_contents` - 교육 콘텐츠

```sql
-- TODO: DB팀 - 교육 콘텐츠 테이블 생성
CREATE TABLE educational_contents (
  id BIGINT PRIMARY KEY AUTO_INCREMENT,
  title VARCHAR(200) NOT NULL,
  subtitle VARCHAR(300),
  description TEXT,
  category VARCHAR(100), -- 기초 교육, 실전 가이드, 심화 학습
  content_type ENUM('video', 'document', 'slide', 'audio', 'article') NOT NULL,
  video_url VARCHAR(500), -- YouTube, Vimeo 등
  file_url VARCHAR(500), -- PDF, PPT 등
  thumbnail VARCHAR(500),
  duration_minutes INT, -- 영상/오디오 길이
  views_count INT DEFAULT 0,
  likes_count INT DEFAULT 0,
  difficulty ENUM('beginner', 'intermediate', 'advanced') DEFAULT 'beginner',
  tags JSON,
  is_featured BOOLEAN DEFAULT FALSE,
  is_published BOOLEAN DEFAULT FALSE,
  published_at TIMESTAMP NULL,
  created_by BIGINT NOT NULL,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,

  FOREIGN KEY (created_by) REFERENCES users(id) ON DELETE CASCADE,
  INDEX idx_category (category),
  INDEX idx_content_type (content_type),
  INDEX idx_is_published (is_published),
  INDEX idx_created_at (created_at)
);
```

#### `news` - 뉴스/공지사항

```sql
-- TODO: DB팀 - 뉴스 테이블 생성
CREATE TABLE news (
  id BIGINT PRIMARY KEY AUTO_INCREMENT,
  title VARCHAR(300) NOT NULL,
  content TEXT NOT NULL,
  category VARCHAR(100), -- 공지사항, 안전가이드, 이벤트, 보도자료 등
  thumbnail VARCHAR(500),
  is_featured BOOLEAN DEFAULT FALSE,
  is_pinned BOOLEAN DEFAULT FALSE, -- 상단 고정
  views_count INT DEFAULT 0,
  author_id BIGINT NOT NULL,
  published_at TIMESTAMP NULL,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,

  FOREIGN KEY (author_id) REFERENCES users(id) ON DELETE CASCADE,
  INDEX idx_category (category),
  INDEX idx_published_at (published_at),
  INDEX idx_is_pinned (is_pinned),
  FULLTEXT INDEX ft_title_content (title, content)
);
```

#### `events` - 이벤트

```sql
-- TODO: DB팀 - 이벤트 테이블 생성
CREATE TABLE events (
  id BIGINT PRIMARY KEY AUTO_INCREMENT,
  title VARCHAR(200) NOT NULL,
  description TEXT,
  event_type ENUM('promotion', 'benefit', 'contest', 'survey') NOT NULL,
  thumbnail VARCHAR(500),
  banner_image VARCHAR(500),
  start_date TIMESTAMP,
  end_date TIMESTAMP,
  reward_description TEXT, -- 혜택 내용
  terms_conditions TEXT, -- 이용 약관
  status ENUM('upcoming', 'active', 'ended') DEFAULT 'upcoming',
  participants_count INT DEFAULT 0,
  max_participants INT, -- 최대 참여 인원
  created_by BIGINT NOT NULL,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,

  FOREIGN KEY (created_by) REFERENCES users(id) ON DELETE CASCADE,
  INDEX idx_status (status),
  INDEX idx_start_date (start_date),
  INDEX idx_event_type (event_type)
);
```

---

### 8. 가이드 시스템 (NEW)

#### `guides` - 가이드 콘텐츠

```sql
-- TODO: DB팀 - 가이드 테이블 생성
CREATE TABLE guides (
  id BIGINT PRIMARY KEY AUTO_INCREMENT,
  category ENUM('입양 신청하기', '서류 제출하기', '기금 납부하기') NOT NULL,
  title VARCHAR(300) NOT NULL,
  description TEXT NOT NULL,
  read_time VARCHAR(20), -- 예: "3분"
  video_url VARCHAR(500), -- YouTube 임베드 URL (선택)
  views_count INT DEFAULT 0,
  likes_count INT DEFAULT 0,
  status ENUM('draft', 'published') DEFAULT 'draft',
  created_by BIGINT NOT NULL,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,

  FOREIGN KEY (created_by) REFERENCES users(id) ON DELETE CASCADE,
  INDEX idx_category (category),
  INDEX idx_status (status),
  INDEX idx_created_at (created_at)
);
```

#### `guide_sections` - 가이드 섹션

```sql
-- TODO: DB팀 - 가이드 섹션 테이블 생성
CREATE TABLE guide_sections (
  id BIGINT PRIMARY KEY AUTO_INCREMENT,
  guide_id BIGINT NOT NULL,
  title VARCHAR(300) NOT NULL,
  section_type ENUM('text', 'video', 'image-explanation', 'list', 'tips') NOT NULL,
  content TEXT, -- 섹션 내용 (선택)
  image_url VARCHAR(500), -- 이미지 URL (선택)
  items JSON, -- 리스트/팁 항목들 (선택)
  display_order INT DEFAULT 0, -- 섹션 표시 순서
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,

  FOREIGN KEY (guide_id) REFERENCES guides(id) ON DELETE CASCADE,
  INDEX idx_guide_id (guide_id),
  INDEX idx_display_order (display_order)
);
```

#### `guide_tags` - 가이드 태그

```sql
-- TODO: DB팀 - 가이드 태그 테이블 생성
CREATE TABLE guide_tags (
  id BIGINT PRIMARY KEY AUTO_INCREMENT,
  guide_id BIGINT NOT NULL,
  tag VARCHAR(50) NOT NULL,

  FOREIGN KEY (guide_id) REFERENCES guides(id) ON DELETE CASCADE,
  INDEX idx_guide_id (guide_id),
  INDEX idx_tag (tag)
);
```

---

### 9. 상호작용 (좋아요, 북마크 등)

#### `likes` - 좋아요

```sql
-- TODO: DB팀 - 좋아요 테이블 생성
CREATE TABLE likes (
  id BIGINT PRIMARY KEY AUTO_INCREMENT,
  user_id BIGINT NOT NULL,
  likeable_type ENUM('post', 'comment', 'question', 'answer', 'educational_content') NOT NULL,
  likeable_id BIGINT NOT NULL,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,

  FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE,
  UNIQUE KEY unique_like (user_id, likeable_type, likeable_id),
  INDEX idx_likeable (likeable_type, likeable_id)
);
```

#### `bookmarks` - 북마크

```sql
-- TODO: DB팀 - 북마크 테이블 생성
CREATE TABLE bookmarks (
  id BIGINT PRIMARY KEY AUTO_INCREMENT,
  user_id BIGINT NOT NULL,
  bookmarkable_type ENUM('post', 'question', 'educational_content', 'news') NOT NULL,
  bookmarkable_id BIGINT NOT NULL,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,

  FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE,
  UNIQUE KEY unique_bookmark (user_id, bookmarkable_type, bookmarkable_id),
  INDEX idx_bookmarkable (bookmarkable_type, bookmarkable_id)
);
```

---

### 9. 통계 및 분석

#### `user_activity_logs` - 사용자 활동 로그

```sql
-- TODO: DB팀 - 활동 로그 테이블 생성
CREATE TABLE user_activity_logs (
  id BIGINT PRIMARY KEY AUTO_INCREMENT,
  user_id BIGINT,
  activity_type VARCHAR(100) NOT NULL, -- page_view, download, registration, etc.
  resource_type VARCHAR(100),
  resource_id BIGINT,
  metadata JSON, -- 추가 정보
  ip_address VARCHAR(45),
  user_agent TEXT,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,

  FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE SET NULL,
  INDEX idx_user_id (user_id),
  INDEX idx_activity_type (activity_type),
  INDEX idx_created_at (created_at)
);
```

#### `statistics_daily` - 일별 통계

```sql
-- TODO: DB팀 - 일별 통계 테이블 생성
CREATE TABLE statistics_daily (
  id BIGINT PRIMARY KEY AUTO_INCREMENT,
  stat_date DATE UNIQUE NOT NULL,
  total_users INT DEFAULT 0,
  new_users INT DEFAULT 0,
  active_users INT DEFAULT 0, -- DAU
  page_views INT DEFAULT 0,
  beach_adoptions INT DEFAULT 0,
  cleanup_activities INT DEFAULT 0,
  waste_collected_kg DECIMAL(10, 2) DEFAULT 0,
  haebom_registrations INT DEFAULT 0,
  questions_submitted INT DEFAULT 0,
  answers_provided INT DEFAULT 0,
  quiz_participations INT DEFAULT 0,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,

  INDEX idx_stat_date (stat_date)
);
```

---

## 🔌 API 엔드포인트 (프론트엔드 참고용)

### 사용자 관리

- `POST /api/auth/register` - 회원가입
- `POST /api/auth/login` - 로그인
- `POST /api/auth/logout` - 로그아웃
- `GET /api/users/me` - 내 정보 조회
- `PUT /api/users/me` - 내 정보 수정

### 반려해변입양

- `POST /api/adoptions` - 입양 신청
- `GET /api/adoptions` - 입양 목록 조회
- `GET /api/adoptions/:id` - 입양 상세 조회
- `POST /api/adoptions/:id/activities` - 정화 활동 기록
- `GET /api/adoptions/:id/activities` - 활동 이력 조회

### 해봄 프로그램

- `GET /api/programs` - 프로그램 목록
- `GET /api/programs/:id` - 프로그램 상세
- `POST /api/programs/:id/register` - 프로그램 신청
- `GET /api/programs/my-registrations` - 내 신청 내역

### 커뮤니티

- `GET /api/boards/:slug/posts` - 게시글 목록
- `GET /api/posts/:id` - 게시글 상세
- `POST /api/boards/:slug/posts` - 게시글 작성
- `PUT /api/posts/:id` - 게시글 수정
- `DELETE /api/posts/:id` - 게시글 삭제
- `POST /api/posts/:id/comments` - 댓글 작성

### 전문가 Q&A

- `GET /api/questions` - 질문 목록
- `GET /api/questions/:id` - 질문 상세
- `POST /api/questions` - 질문 작성
- `POST /api/questions/:id/answers` - 답변 작성 (전문가)
- `GET /api/questions/popular` - 인기 질문 TOP 5

### 참여광장

- `GET /api/quizzes` - 퀴즈 목록
- `GET /api/quizzes/:id` - 퀴즈 상세
- `POST /api/quizzes/:id/submit` - 퀴즈 제출
- `GET /api/polls` - 투표 목록
- `POST /api/polls/:id/vote` - 투표 참여

### 콘텐츠

- `GET /api/educational-contents` - 교육 콘텐츠 목록
- `GET /api/educational-contents/:id` - 교육 콘텐츠 상세
- `GET /api/news` - 뉴스/공지 목록
- `GET /api/news/:id` - 뉴스/공지 상세
- `GET /api/events` - 이벤트 목록

### 가이드 (NEW)

- `GET /api/guides` - 가이드 목록 (공개용)
- `GET /api/guides/:id` - 가이드 상세
- `GET /api/admin/guides` - 가이드 관리 목록 (관리자)
- `GET /api/admin/guides/:id` - 가이드 조회 (관리자)
- `POST /api/admin/guides` - 가이드 생성
- `PUT /api/admin/guides/:id` - 가이드 수정
- `DELETE /api/admin/guides/:id` - 가이드 삭제

### 상호작용

- `POST /api/likes` - 좋아요
- `DELETE /api/likes/:id` - 좋아요 취소
- `POST /api/bookmarks` - 북마크 추가
- `DELETE /api/bookmarks/:id` - 북마크 제거

---

## 📝 DB 팀 체크리스트

### Phase 1 (MVP)

- [ ] TODO: 사용자 관리 테이블 생성 (users)
- [ ] TODO: 반려해변입양 테이블 생성 (beach_adoptions, cleanup_activities)
- [ ] TODO: 해봄 프로그램 테이블 생성 (haebom_programs, haebom_registrations)
- [ ] TODO: 게시판 테이블 생성 (boards, posts, comments)
- [ ] TODO: 기본 API 엔드포인트 구현
- [ ] TODO: 데이터베이스 인덱스 최적화
- [ ] TODO: 백업 정책 수립

### Phase 2 (확장)

- [ ] TODO: 전문가 Q&A 테이블 생성 (experts, questions, answers)
- [ ] TODO: 퀴즈/투표 테이블 생성 (quizzes, quiz_responses, polls, poll_votes)
- [ ] TODO: 콘텐츠 관리 테이블 생성 (educational_contents, news, events)
- [ ] TODO: 가이드 시스템 테이블 생성 (guides, guide_sections, guide_tags)
- [ ] TODO: 가이드 관리 API 엔드포인트 구현
- [ ] TODO: 좋아요/북마크 테이블 생성 (likes, bookmarks)
- [ ] TODO: Full-text search 최적화
- [ ] TODO: 캐싱 전략 구현 (Redis)

### Phase 3 (고도화)

- [ ] TODO: 통계 테이블 생성 (user_activity_logs, statistics_daily)
- [ ] TODO: 실시간 데이터 집계 배치 작업
- [ ] TODO: 데이터 아카이빙 정책
- [ ] TODO: 성능 모니터링 대시보드
- [ ] TODO: 데이터 마이그레이션 스크립트

---

## 🔐 보안 고려사항

### 개인정보 보호

- [ ] TODO: 비밀번호는 bcrypt 등으로 암호화하여 저장
- [ ] TODO: 개인정보 필드(email, phone) 암호화 검토
- [ ] TODO: 개인정보 접근 로그 기록
- [ ] TODO: GDPR/개인정보보호법 준수 체크

### 데이터 무결성

- [ ] TODO: Foreign Key 제약조건 설정
- [ ] TODO: 트랜잭션 처리 정책
- [ ] TODO: 데이터 백업 자동화 (일 1회)
- [ ] TODO: Soft delete vs Hard delete 정책 수립

### 접근 제어

- [ ] TODO: 역할 기반 접근 제어 (RBAC) 구현
- [ ] TODO: API Rate Limiting
- [ ] TODO: SQL Injection 방어
- [ ] TODO: 감사 로그 (Audit Log) 시스템

---

## 📊 성능 최적화

### 인덱스 전략

```sql
-- TODO: DB팀 - 자주 조회되는 필드에 인덱스 추가
-- 예시:
-- - users: email, user_type, created_at
-- - posts: board_id, status, created_at, is_notice
-- - questions: category, status, created_at, is_featured
-- - cleanup_activities: adoption_id, activity_date
```

### 쿼리 최적화

- [ ] TODO: N+1 쿼리 문제 해결 (JOIN 사용)
- [ ] TODO: 페이지네이션 성능 개선 (커서 기반)
- [ ] TODO: 복잡한 집계 쿼리 최적화 (Materialized View 검토)

### 캐싱 전략

- [ ] TODO: Redis 캐싱 레이어 구현
  - 인기 질문 TOP 5 (1시간 캐시)
  - 교육 콘텐츠 목록 (30분 캐시)
  - 사용자 세션 (토큰 기반)
- [ ] TODO: CDN 연동 (이미지, 동영상)

---

## 🚀 배포 및 운영

### 환경 설정

```bash
# TODO: DB팀 - 환경변수 설정
DB_HOST=localhost
DB_PORT=3306
DB_NAME=lighthouse_service
DB_USER=lighthouse_user
DB_PASSWORD=<secure_password>
DB_CHARSET=utf8mb4
DB_COLLATION=utf8mb4_unicode_ci
```

### 마이그레이션

- [ ] TODO: 마이그레이션 도구 선정 (Flyway, Liquibase 등)
- [ ] TODO: 개발/스테이징/프로덕션 환경 분리
- [ ] TODO: 롤백 시나리오 준비

### 모니터링

- [ ] TODO: 슬로우 쿼리 로그 활성화
- [ ] TODO: DB 성능 모니터링 도구 (Datadog, New Relic 등)
- [ ] TODO: 디스크 용량 알림 설정
- [ ] TODO: 커넥션 풀 모니터링

---

## 📞 문의

### DB 팀 담당자

- **이메일**: db-team@lighthouse-service.org
- **Slack**: #db-team

### 프론트엔드 팀

- **이메일**: frontend-team@lighthouse-service.org
- **Slack**: #frontend-dev

---

**문서 버전**: 1.0  
**최종 업데이트**: 2026.01.16  
**다음 리뷰 예정일**: 2026.02.01
